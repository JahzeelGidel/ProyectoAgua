<FiltroWidget>:
    orientation: "vertical"
    padding: 20
    spacing: 15
    canvas.before:
        Color:
            rgba: 0.9, 0.95, 1, 1
        Rectangle:
            pos: self.pos
            size: self.size

    Label:
        text: "💧 Gota de esperanza"
        font_size: "26sp"
        color: 0, 0.2, 0.6, 1
        bold: True
        size_hint_y: None
        height: self.texture_size[1] + 10

    BoxLayout:
        orientation: "vertical"
        padding: [10, 0]
        spacing: 10

        Label:
            text: f"Presión antes: {root.presion_antes}"
            font_size: "20sp"
            color: 0, 0, 0.3, 1

        Label:
            text: f"Presión después: {root.presion_despues}"
            font_size: "20sp"
            color: 0, 0, 0.3, 1

    Label:
        text: root.estado_texto
        font_size: "24sp"
        bold: True
        color: root.color_estado
        size_hint_y: None
        height: self.texture_size[1] + 20

    Button:
        text: "💦 Liberar Agua del Filtro"
        font_size: "20sp"
        bold: True
        background_color: 0, 0.6, 1, 1
        color: 1, 1, 1, 1
        size_hint: (1, None)
        height: 60
        on_press: root.liberar_agua()
